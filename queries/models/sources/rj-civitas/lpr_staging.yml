version: 2

sources:
  - name: lpr_staging
    database: >
      {% if target.name == 'staging' %}
      rj-civitas-dev
      {% else %}
      rj-civitas
      {% endif %}
    schema: lpr_staging
    tables:
      - name: lanes