version: 2

sources:
  - name: lpr
    database: >
      {% if target.name == 'staging' %}
      rj-civitas-dev
      {% else %}
      rj-civitas
      {% endif %}
    schema: lpr
    tables:
      - name: readings_raw
        identifier: readings_*
      - name: cameras
